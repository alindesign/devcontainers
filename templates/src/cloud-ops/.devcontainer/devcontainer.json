{
  "name": "Cloud Ops (alindesign)",
  "image": "ghcr.io/alindesign/devcontainer-base:latest",
  "features": {
    "ghcr.io/alindesign/features/aws-cli:1": {},
    "ghcr.io/alindesign/features/gcloud:1": {
      "installComponents": "${templateOption:gcloudComponents}"
    }
  },
  "remoteUser": "vscode",
  "mounts": [
    { "source": "${localEnv:HOME}/.aws", "target": "/home/vscode/.aws", "type": "bind" },
    { "source": "${localEnv:HOME}/.config/gcloud", "target": "/home/vscode/.config/gcloud", "type": "bind" }
  ],
  "postCreateCommand": "bash -c 'gn=\"${templateOption:gitUserName}\"; ge=\"${templateOption:gitUserEmail}\"; [ -n \"$gn\" ] && git config --global user.name \"$gn\"; [ -n \"$ge\" ] && git config --global user.email \"$ge\"; aws --version && gcloud --version | head -n1'",
  "customizations": {
    "vscode": {
      "extensions": [
        "amazonwebservices.aws-toolkit-vscode",
        "googlecloudtools.cloudcode",
        "hashicorp.terraform"
      ]
    }
  }
}
