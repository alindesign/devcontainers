{
  "id": "aws-cli",
  "version": "1.0.1",
  "name": "AWS CLI v2",
  "description": "Installs the official AWS CLI v2 binary. Mount ~/.aws from the host in your devcontainer.json to reuse host credentials (see README).",
  "documentationURL": "https://github.com/alindesign/devcontainers/tree/main/src/aws-cli",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "AWS CLI v2 version. 'latest' or a specific version like '2.18.0'."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
