{
  "id": "gcloud",
  "version": "1.0.1",
  "name": "Google Cloud CLI",
  "description": "Installs gcloud CLI via Google's official apt repository. Mount ~/.config/gcloud from the host in your devcontainer.json to reuse host logins (see README).",
  "documentationURL": "https://github.com/alindesign/devcontainers/tree/main/src/gcloud",
  "options": {
    "installComponents": {
      "type": "string",
      "default": "",
      "description": "Space-separated additional gcloud components to install (e.g. 'kubectl gke-gcloud-auth-plugin terraform-tools')."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
